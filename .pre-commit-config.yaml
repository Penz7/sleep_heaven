repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: gitleaks staged scan
        entry: gitleaks protect --staged --config .gitleaks.toml --redact --verbose
        language: system
        pass_filenames: false
