---
phase: 02-reliability-and-observability-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/core/services/audio_handler.dart
  - lib/features/player/controllers/player_controller.dart
  - lib/features/mixer/controllers/mixer_controller.dart
  - test/services/audio/audio_handoff_contract_test.dart
autonomous: true
requirements:
  - P2-REL-AUDIO
must_haves:
  truths:
    - "Only one active controller owns audio handler callbacks at any moment."
    - "Ownership handoff between player and mixer is deterministic and never leaves stale callback routing."
    - "Lock-screen/notification play/pause controls stay aligned to the current active owner."
  artifacts:
    - path: test/services/audio/audio_handoff_contract_test.dart
      provides: ownership handoff contract tests across player/mixer transitions.
    - path: lib/core/services/audio_handler.dart
      provides: single-owner callback contract enforcement surface.
    - path: lib/features/player/controllers/player_controller.dart
      provides: player ownership registration/cleanup aligned to contract.
    - path: lib/features/mixer/controllers/mixer_controller.dart
      provides: mixer ownership registration/cleanup aligned to contract.
  key_links:
    - from: lib/features/player/controllers/player_controller.dart
      to: lib/core/services/audio_handler.dart
      via: ownership assignment and release
      pattern: "onPlayRequested|onPauseRequested"
    - from: lib/features/mixer/controllers/mixer_controller.dart
      to: lib/core/services/audio_handler.dart
      via: ownership assignment and release
      pattern: "onPlayRequested|onPauseRequested"
    - from: test/services/audio/audio_handoff_contract_test.dart
      to: lib/core/services/audio_handler.dart
      via: latest-owner-wins and stale-callback prevention assertions
      pattern: "handoff|owner|notification"
---

<objective>
Harden the single-owner audio control contract with deterministic handoff behavior and contract tests.

Purpose: Implement locked audio lifecycle decisions (D-07, D-08, D-09) and prevent orphan callback regressions in player/mixer ownership transitions.
Output: Enforced ownership contract behavior in audio handler/controllers plus repeatable handoff tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reliability-and-observability-baseline/02-CONTEXT.md
@.planning/phases/02-reliability-and-observability-baseline/02-RESEARCH.md
@.planning/phases/02-reliability-and-observability-baseline/02-01-PLAN.md
@lib/core/services/audio_handler.dart
@lib/features/player/controllers/player_controller.dart
@lib/features/mixer/controllers/mixer_controller.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create explicit audio ownership handoff contract tests</name>
  <files>test/services/audio/audio_handoff_contract_test.dart</files>
  <behavior>
    - Test 1: player ownership registers callbacks and receives control events when active owner.
    - Test 2: mixer ownership takeover invalidates prior player callback routing (latest owner wins) per D-08.
    - Test 3: ownership release/transition never leaves ambiguous callback state for lock-screen controls per D-09.
  </behavior>
  <action>Build deterministic contract tests for player/mixer handoff sequences around `SleepAudioHandler` callback ownership, including re-entrant transitions and dispose paths to catch orphan/stale callback regressions per D-08.</action>
  <verify>
    <automated>flutter test test/services/audio/audio_handoff_contract_test.dart</automated>
  </verify>
  <done>Ownership handoff expectations are encoded as stable tests that fail on stale callback routing or ambiguous ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce single-owner callback contract in audio handler</name>
  <files>lib/core/services/audio_handler.dart</files>
  <action>Harden `SleepAudioHandler` callback ownership API to enforce one active owner at a time per D-07, with explicit handoff semantics that prevent orphan callbacks and ambiguous states. Preserve existing external behavior while making ownership transitions deterministic for contract test compatibility.</action>
  <verify>
    <automated>flutter test test/services/audio/audio_handoff_contract_test.dart</automated>
  </verify>
  <done>Audio handler guarantees single-owner callback routing with deterministic handoff behavior and passing contract tests.</done>
</task>

<task type="auto">
  <name>Task 3: Align player and mixer controllers to ownership lifecycle contract</name>
  <files>lib/features/player/controllers/player_controller.dart, lib/features/mixer/controllers/mixer_controller.dart</files>
  <action>Update controller ownership registration and cleanup paths to strictly follow the hardened audio handler contract (acquire, transfer, release) and preserve lock-screen/notification correctness during screen changes per D-09.</action>
  <verify>
    <automated>flutter test test/services/audio/audio_handoff_contract_test.dart</automated>
  </verify>
  <done>Player/mixer transitions consistently route controls to the active owner with no stale callbacks after handoff or dispose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI controller lifecycle -> shared audio handler | Competing owners update shared callback state |
| notification/lock-screen commands -> controller callbacks | External media controls invoke app callback routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | T | audio callback ownership | mitigate | enforce single-owner contract with explicit handoff semantics and tests |
| T-02-08 | D | control routing after screen transitions | mitigate | controller lifecycle acquire/release discipline validated by contract tests |
| T-02-09 | E | stale owner receiving privileged controls | mitigate | invalidate previous owner callbacks on takeover and assert latest-owner routing |
</threat_model>

<verification>
Run audio ownership reliability suite:
- `flutter test test/services/audio/audio_handoff_contract_test.dart`
</verification>

<success_criteria>
- Single-owner callback contract is enforced in `SleepAudioHandler`.
- Player/mixer ownership handoffs are deterministic and contract-tested.
- Lock-screen/notification control routing remains correct through repeated transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability-and-observability-baseline/02-03-SUMMARY.md`
</output>
