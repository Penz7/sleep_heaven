---
phase: 02-reliability-and-observability-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/core/services/iap_service.dart
  - test/services/iap/iap_service_reliability_test.dart
  - test/startup/iap_startup_restore_reconciliation_test.dart
autonomous: true
requirements:
  - P2-REL-IAP
must_haves:
  truths:
    - "IAP reliability coverage deterministically validates five required states."
    - "Entitlement cache and purchase stream reconciliation is consistent on startup and restore."
    - "Offline restore transitions fail safely and recover when connectivity returns."
  artifacts:
    - path: test/services/iap/iap_service_reliability_test.dart
      provides: deterministic matrix tests for `not_purchased`,`purchased`,`restored`,`failed`,`offline_restore`.
    - path: test/startup/iap_startup_restore_reconciliation_test.dart
      provides: startup/offline-online reconciliation coverage.
    - path: lib/core/services/iap_service.dart
      provides: deterministic state handling and reconciliation hooks under singleton contract.
  key_links:
    - from: lib/core/services/iap_service.dart
      to: test/services/iap/iap_service_reliability_test.dart
      via: contract-level state transition assertions
      pattern: "purchase|restore|entitlement"
    - from: lib/core/services/iap_service.dart
      to: test/startup/iap_startup_restore_reconciliation_test.dart
      via: startup restore behavior assertions
      pattern: "startup|offline|online"
---

<objective>
Lock IAP reliability behavior behind deterministic 5-state automated coverage and startup reconciliation tests.

Purpose: Implement locked IAP reliability decisions (D-04, D-05, D-06) without architecture refactor risk.
Output: Deterministic IAP test harness and targeted singleton-safe service updates in `IAPService`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reliability-and-observability-baseline/02-CONTEXT.md
@.planning/phases/02-reliability-and-observability-baseline/02-RESEARCH.md
@.planning/phases/02-reliability-and-observability-baseline/02-01-PLAN.md
@lib/core/services/iap_service.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic 5-state IAP reliability test matrix</name>
  <files>test/services/iap/iap_service_reliability_test.dart</files>
  <behavior>
    - Test 1: `not_purchased` keeps entitlement false and no premium leakage.
    - Test 2: `purchased` sets entitlement true and persists cache safely.
    - Test 3: `restored` restores entitlement idempotently across duplicate events.
    - Test 4: `failed` preserves safe entitlement state and emits recoverable error signal.
    - Test 5: `offline_restore` keeps safe degraded state and marks pending reconciliation.
  </behavior>
  <action>Create deterministic tests covering exactly the five mandatory states per D-04 with explicit expected entitlement/cache outcomes. Use a controlled fake stream/store harness so state transitions are reproducible across repeated runs.</action>
  <verify>
    <automated>flutter test test/services/iap/iap_service_reliability_test.dart</automated>
  </verify>
  <done>All five mandatory IAP states are encoded as stable automated tests with unambiguous expected outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add startup restore offline/online reconciliation coverage</name>
  <files>test/startup/iap_startup_restore_reconciliation_test.dart</files>
  <behavior>
    - Test 1: startup restore while offline does not grant entitlement and records pending reconciliation.
    - Test 2: transition to online replays restore and reconciles stream/cache consistently.
    - Test 3: repeated startup restore attempts remain idempotent with singleton lifetime service.
  </behavior>
  <action>Add startup-focused tests for entitlement cache plus purchase stream reconciliation per D-05, explicitly modeling offline then online transitions and ensuring deterministic reconciliation behavior.</action>
  <verify>
    <automated>flutter test test/startup/iap_startup_restore_reconciliation_test.dart</automated>
  </verify>
  <done>Startup restore reconciliation under offline/online transitions is test-covered and deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Implement minimal singleton-safe IAP service updates to satisfy matrix</name>
  <files>lib/core/services/iap_service.dart</files>
  <action>Update `IAPService` internals only as required to pass the matrix tests while preserving app-lifetime singleton contract per D-06 (no architecture refactor). Ensure state transitions and cache reconciliation paths are deterministic and tagged for telemetry/error model integration introduced in 02-01.</action>
  <verify>
    <automated>flutter test test/services/iap/iap_service_reliability_test.dart test/startup/iap_startup_restore_reconciliation_test.dart</automated>
  </verify>
  <done>IAP singleton behavior satisfies all five mandatory states and startup reconciliation coverage without contract-breaking refactor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store purchase stream -> entitlement state | Untrusted external billing events alter premium access policy |
| local cache -> runtime entitlement decisions | Persisted state may diverge from live purchase events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | iap entitlement reconciliation | mitigate | explicit state matrix tests and startup reconciliation logic for cache/stream divergence |
| T-02-05 | D | offline restore handling | mitigate | fail-safe entitlement defaults plus online retry reconciliation path |
| T-02-06 | R | purchase transition diagnostics | mitigate | deterministic state tagging and test assertions for each mandatory state transition |
</threat_model>

<verification>
Run IAP reliability suite:
- `flutter test test/services/iap/iap_service_reliability_test.dart`
- `flutter test test/startup/iap_startup_restore_reconciliation_test.dart`
</verification>

<success_criteria>
- Five mandatory IAP states are reproducible and automated-test covered.
- Startup restore reconciliation is deterministic for offline -> online transitions.
- `IAPService` singleton contract remains intact while reliability behavior is hardened.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability-and-observability-baseline/02-02-SUMMARY.md`
</output>
