---
phase: 05-feature-growth-readiness-iap-catalog-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/config/feature_flags.dart
  - lib/core/config/feature_flag_service.dart
  - lib/main.dart
  - test/config/feature_flag_service_test.dart
  - test/startup/feature_flag_bootstrap_test.dart
autonomous: true
requirements:
  - P5-FLAG-01
must_haves:
  truths:
    - "Premium feature rollout toggles can be turned on/off without rebuilding the app binary."
    - "Feature flags load deterministically at startup and are queryable across controllers/services."
    - "When a flag is missing/invalid, app behavior stays fail-safe with default-off premium gates."
  artifacts:
    - path: lib/core/config/feature_flags.dart
      provides: Typed feature flag keys/defaults for premium rollout controls.
    - path: lib/core/config/feature_flag_service.dart
      provides: Runtime feature flag resolver with deterministic default/fallback behavior.
    - path: test/config/feature_flag_service_test.dart
      provides: Contract coverage for valid/missing/invalid flag payload behavior.
    - path: test/startup/feature_flag_bootstrap_test.dart
      provides: Startup bootstrap proof that flags load before premium-gated flows execute.
  key_links:
    - from: lib/main.dart
      to: lib/core/config/feature_flag_service.dart
      via: startup initialization
      pattern: "FeatureFlagService"
    - from: lib/core/config/feature_flag_service.dart
      to: lib/core/config/feature_flags.dart
      via: typed key lookup and default fallback
      pattern: "FeatureFlags"
---

<objective>
Create a production-safe feature-flag foundation for premium capability rollout controls in Phase 5.

Purpose: Deliver requirement P5-FLAG-01 by making premium feature release controls runtime-configurable, testable, and fail-safe.
Output: Typed flag contract, startup-integrated flag service, and deterministic tests proving runtime and startup behavior.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/main.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed feature-flag contracts and defaults</name>
  <files>lib/core/config/feature_flags.dart, test/config/feature_flag_service_test.dart</files>
  <behavior>
    - Test 1: known premium flag keys resolve to expected typed values when present.
    - Test 2: missing flag keys always return documented default values (default-off for premium gates).
    - Test 3: invalid payload values fail closed and do not enable premium functionality.
  </behavior>
  <action>Create `FeatureFlags` constants and typed value access contracts for premium rollout controls per P5-FLAG-01. Add unit tests first to lock fail-closed behavior for missing/invalid values and avoid permissive fallback drift.</action>
  <verify>
    <automated>flutter test test/config/feature_flag_service_test.dart</automated>
  </verify>
  <done>Feature flag key space is explicit, typed, and test-guarded with default-off behavior for premium features.</done>
</task>

<task type="auto">
  <name>Task 2: Implement startup-integrated feature flag resolver</name>
  <files>lib/core/config/feature_flag_service.dart, lib/main.dart, test/startup/feature_flag_bootstrap_test.dart</files>
  <action>Implement `FeatureFlagService` and initialize it during app bootstrap in `main.dart` before premium-gated flows run. Keep integration local-first (asset/env-backed resolver) and deterministic, with startup smoke tests ensuring fallback-safe behavior when runtime config is absent/corrupt per P5-FLAG-01.</action>
  <verify>
    <automated>flutter test test/config/feature_flag_service_test.dart test/startup/feature_flag_bootstrap_test.dart</automated>
  </verify>
  <done>App startup always has a usable flag resolver, and premium flags can be toggled without rebuild while invalid config fails closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime flag source -> app bootstrap | Untrusted flag payloads influence premium gating behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | feature flag payload parsing | mitigate | strict typed parsing with default-off fallback for invalid/missing values |
| T-05-02 | E | premium gate evaluation | mitigate | centralize flag checks in service, no ad-hoc bypass checks in UI/controllers |
| T-05-03 | D | startup bootstrap | mitigate | initialize flag service in startup path with deterministic fallback to avoid boot failure |
</threat_model>

<verification>
- `flutter test test/config/feature_flag_service_test.dart`
- `flutter test test/startup/feature_flag_bootstrap_test.dart`
</verification>

<success_criteria>
- Premium rollout controls are runtime-configurable through a typed, test-guarded flag service.
- Missing/invalid flag payloads cannot accidentally enable premium capabilities.
- Startup always initializes feature flags before premium-gated logic executes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-feature-growth-readiness-iap-catalog-evolution/05-01-SUMMARY.md`
</output>
